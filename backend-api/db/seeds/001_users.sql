-- Seed: Default users (admin + agent)
-- Passwords are bcrypt-hashed via the seed script (not raw SQL)
-- This file documents the seed data; actual seeding uses db/seed.js

-- admin@example.com / Admin123!  (role: admin)
-- agent@example.com / Agent123!  (role: agent)
